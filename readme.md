# Praktika game

Запуск: `npm run dev`  
  
Билд: `npm run build`  
После этого можно запускать командами:  
`python -m http.server 3000 --directory dist`  
`php -S localhost:3000 -t dist`  
`npx http-server dist --cors`  